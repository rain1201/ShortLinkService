# Pin this to a published, immutable application image tag.
SHORTLINK_IMAGE_TAG=main

# Set a strong password before starting the stack.
REDIS_PASSWORD=change-me

# HSQLDB uses an empty password by default in this project.
SPRING_DATASOURCE_PASSWORD=
